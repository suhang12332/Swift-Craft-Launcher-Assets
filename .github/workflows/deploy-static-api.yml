name: Deploy Static API

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate API
        run: |
          cd news
          node scripts/generate-api.js
          echo "Generated files:"
          find api -name "*.json" | head -20

      - name: Commit and push API files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          echo "Checking for changes in news/api/"
          git status news/api/
          git add -A news/api/
          git status
          if ! git diff --staged --quiet; then
            git commit -m "Update API files [skip ci]"
            git pull --rebase origin main
            git push origin HEAD:main
          else
            echo "No changes to commit"
          fi

  update-releases:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Generate release HTML files
        run: |
          python .github/scripts/fetch_swift_craft_releases.py

      - name: Commit and push release files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          echo "Checking for changes in releases/"
          git status releases/
          git add -A releases/
          git status
          if ! git diff --staged --quiet; then
            git commit -m "Update release HTML files [skip ci]"
            git pull --rebase origin main
            git push origin HEAD:main
          else
            echo "No changes to commit"
          fi

